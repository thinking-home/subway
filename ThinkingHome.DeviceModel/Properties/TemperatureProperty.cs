namespace ThinkingHome.DeviceModel.Properties;

/// <summary>Температура, °C — нормализованная единица ядра. Matter cluster Temperature Measurement (0x0402). Instance — "temperature".</summary>
public sealed record TemperatureProperty : Property;
