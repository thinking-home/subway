# Caddy сам получит и будет обновлять сертификат Let's Encrypt, а также
# проксирует WebSocket (нужно для SignalR /hub) по умолчанию.
# Замените proxy.example.com на свой домен (A-запись должна указывать на этот сервер).
proxy.example.com {
	reverse_proxy 127.0.0.1:8080
}
